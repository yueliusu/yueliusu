# 纸面上的技术阅读入口

沿用 src/index.css 与 desk.tsx 中已有暖纸色、墨色、手绘划线、轻微倾斜便签日期、头像贴纸与蓝色批注。此次为现有视觉的扩展，使用代码实现。

首页保持单栏书页布局，版心适当扩至 46rem。中文标题、正文和说明建立清楚的层级；正文约 16px，说明不低于 14px，等宽字体限日期和少量注记。主题以分隔行和展开目录呈现，避免增加成排大卡片。

首页顺序：中文作者简介与阅读入口 → 精选必读 → 四类专题 → 最近文章 → 作者背景与联系。完整目录使用静态站可直接访问的 hash 地址，提供关键词、主题、年份筛选、结果反馈与清空。

窄屏下日期和标签下移，正文占满可用宽度，导航换行。所有控件有可见焦点与足够触摸高度。motion-reduce 下不播放位移动画；专题展开用既有折纸动画，保留作者识别度。

公众号使用可关闭原生 dialog，支持 Escape、焦点恢复与触屏操作。所有阅读入口标示 CSDN，完整文章在新标签打开。
